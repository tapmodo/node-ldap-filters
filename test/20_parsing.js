var should = require('chai').should();
var Filter = require('../index');

describe('Parsing',function(){

  it('parse small filter');
  it('parse more complex filter');
  it('perform a match against a parsed filter');

});

