var should = require('chai').should();
var Filter = require('../index');

describe('Matching',function(){
  
  it('simple values');
  it('multi-valued keys');
  it('values that require escaping');
  it('basic approx test ("sounds like")');
  it('complex filter and object match');

});

